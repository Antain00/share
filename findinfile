Voici un script Python qui recherche un mot-clé donné dans tous les fichiers d'un répertoire spécifié. Il parcourt chaque fichier du répertoire, trouve les occurrences du mot-clé et affiche les lignes où elles apparaissent, ainsi que le nom du fichier correspondant.

### Script Python : Recherche de mot-clé dans un répertoire

```python
import os

def chercher_occurrences(dossier, mot_cle):
    """
    Recherche les occurrences d'un mot-clé dans tous les fichiers d'un répertoire donné.
    
    :param dossier: Le chemin du répertoire où effectuer la recherche
    :param mot_cle: Le mot-clé à rechercher
    """
    for root, dirs, files in os.walk(dossier):
        for fichier in files:
            chemin_fichier = os.path.join(root, fichier)
            try:
                with open(chemin_fichier, 'r', encoding='utf-8') as f:
                    lignes = f.readlines()
                    for numero, ligne in enumerate(lignes, 1):
                        if mot_cle in ligne:
                            print(f"Fichier: {chemin_fichier} | Ligne {numero} : {ligne.strip()}")
            except Exception as e:
                print(f"Erreur de lecture du fichier {chemin_fichier} : {e}")

# Utilisation du script
dossier = "/chemin/vers/votre/dossier"  # Remplacez par le chemin de votre répertoire
mot_cle = "votre_mot
